
backend:
    playlist:
        sort
        reverse?
        filter
        dynamic

    queue:
        rebase on new playlist


ui:
    playlist:
        save to saved playlists
        columns respect settings
        columns context-menu
        tracks context-menu
        link up dynamic
        link up search
        search field clear button
        SPAT
            grey out tracks below SPAT designation
                - cell.set_sensitive(False)
                - only if (not shuffling AND current_pos <= spat_pos)
        accept dropped tracks/uris
        DnD to reorder
        rename in dialog/messagebar if tabs are hidden

    tabs:
        middle-click empty space to create new
        support tabs as DnD targets
        tab reordering
    
    queue:
        create new ui for queue in a tab
        make queue tab show itself whenever items are enqueued
        count of queued items in tab



random:
    CellRendererToggleImage - http://pastebin.com/NNCu207Y
